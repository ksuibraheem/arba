# خريطة الثغرات الأولية — تحليل فقط (لا إصلاح)

مبنية على فحص قراءة فقط لـ `firestore.rules` و`usageTrackingService.ts`. تكمّلها فحوصات Antigravity (A1/A2/A5/B6).

## 🔴 حرجة

**V1 — تخطّي الحصة ما زال مفتوحاً (إصلاح P0 وقع على الهدف الخطأ).**
`usageTrackingService.ts` (سطر 234، 298، 326-327) يكتب العدادات في **`users/{userId}`** عبر `increment()`، **لا** في مجموعة `usageTracking`. وقاعدة `/users/{userId}` تسمح `allow update: if request.auth.uid == userId` (سطر 155) → المستخدم يقدر يصفّر/يضخّم حصة الذكاء والتخزين مباشرة. تحصين P0 حمى `usageTracking` (غير مستخدمة). **الثغرة الأصلية قائمة.**

**V2 — تصعيد صلاحيات جزئي قائم.** مسكّن P0 يمنع admin/superadmin فقط؛ المستخدم لا يزال يقدر يعيّن لنفسه `qs_engineer` (يخوّله إنشاء مشاريع وكتابة عروض). الإغلاق التام = Custom Claims.

## 🟠 متوسطة — تزوير سجلات (نمط `create: if isAuth()` بلا فحص ملكية)

أي مستخدم مُصادَق يقدر يحقن سجلات في نطاق غيره أو يلوّث سجلات يفترض أنها غير قابلة للعبث:

| المجموعة | السطر | الأثر |
|---|---|---|
| `invoices` | ~298 | تزوير فواتير |
| `ledger_entries` | ~312 | حقن قيود محاسبية |
| `action_logs` | 207 | تلويث سجل التدقيق (يفترض غير قابل للعبث) |
| `audit_trail` / `price_time_series` / `usage_metering` | 599-617 | حقن في سجلات الامتثال/الأسعار |
| `security_alerts` | 198 | تزييف/إغراق تنبيهات أمنية |
| `supplier_payments` | 360 | حقن دفعات موردين |
| `internal_metadata` | 634 | كتابة ميتاداتا داخلية (create,update: if isAuth()) |

**V3 — `registration_requests` create: `if true` (سطر 282).** كتابة غير مُصادَقة تماماً → ناقل إغراق/إساءة. (مقصود للتسجيل، لكن `if true` فضفاض).

## 🟡 منخفضة

**V4 — `projects` create (سطر 84)** لا يثبّت `ownerId == auth.uid` → مهندس يقدر ينشئ مشروعاً بمالك عشوائي.

**V5 — `stateSnapshot` على وثيقة المشروع يقرأه SuperAdmin** → يلتف على حجب `calculations` (كامل حالة التسعير مكشوفة).

## ✅ تأكيدات
- ادعاء التقرير "usageTracking ميتة" **مضلّل**: خدمة التتبّع حية، لكن تخزينها في `users` (هذا جذر V1).
- رموز `\` في عرض القواعد = أثر عرض الأداة فقط (الاختبارات السلوكية 15/15 نجحت، فالقواعد سليمة الصياغة).

## ⏳ لم يُفحص بعد (يحتاج أدوات Antigravity)
- **A1** قائمة الكود الميت الموثوقة (knip/ts-prune بدل grep يدوي).
- **A2** خريطة قراءة/كتابة فعلية لكل الـ50 مجموعة.
- **A5** اتصال كل Cloud Function + نشر الـ CRONs.
- **B6** `tsc --noEmit` على المشروع كامل (أخطاء أنواع كامنة).
