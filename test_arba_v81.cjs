/**
 * ARBA V8.1 — Integration Test
 * يختبر خط الأنابيب الكامل بملف Excel مُصنّع يحتوي بنود حقيقية
 */

const xlsx = require('xlsx');
const path = require('path');
const ArbaOrchestrator = require('./arba_orchestrator.cjs');

// إنشاء ملف BOQ اختباري يحاكي كراسة كميات حقيقية
function createTestBOQ() {
    const testItems = [
        [1, 'توريد وتركيب بلوك خرساني سماكة 20 سم للجدران الخارجية', 'م2', 3500],
        [2, 'ت.و.ت بلوك خرساني سماكة 15 سم للجدران الداخلية', 'م2', 2800],
        [3, 'لياسة خارجية بسماكة 2 سم شاملة الطرطشة', 'م2', 4200],
        [4, 'لياسة داخلية للجدران والأسقف', 'م2', 6500],
        [5, 'دهان بلاستيك داخلي 3 أوجه شامل المعجون والبرايمر', 'م2', 8000],
        [6, 'دهان خارجي اكريليك مقاوم للعوامل الجوية', 'م2', 3200],
        [7, 'خراسانه مسلحة للقواعد المنفصلة', 'م3', 450],
        [8, 'خرسانة مسلحة للميدات الأرضية', 'م3', 380],
        [9, 'أعمال حفر للأساسات', 'م3', 1200],
        [10, 'ردم ودمك بمواد مختارة', 'م3', 800],
        [11, 'ت.و.ت بورسلان أرضيات 60×60', 'م2', 2500],
        [12, 'توريد وتركيب سراميك جدران حمامات', 'م2', 1800],
        [13, 'توريد وتركيب باب حريق مقاوم 2 ساعة', 'عدد', 12],
        [14, 'توريد وتركيب باب خشب داخلي', 'عدد', 45],
        [15, 'توريد وتركيب نافذة المنيوم عازل حراري مقاس 2x1.5م', 'م2', 350],
        [16, 'تركيب مكيف سبلت 2 طن', 'عدد', 28],
        [17, 'تركيب جبس بورد للأسقف المستعارة', 'م2', 1500],
        [18, 'توريد وتركيب مصعد كهربائي 8 أشخاص', 'عدد', 2],
        [19, 'أعمال إسفلت للمواقف والطرق', 'م2', 4500],
        [20, 'توريد وتركيب مرحاض إفرنجي شامل الإكسسوارات', 'عدد', 35],
        [21, 'بند غامض: أعمال متفرقة عامة', 'مقطوعي', 1],
        [22, 'توريد بلوك 20 سم لا يشمل الأسمنت والمونة', 'م2', 2000],
        [23, 'دهان إيبوكسي للمطابخ والحمامات', 'م2', 900],
        [24, 'حجر رياض طبيعي للواجهات', 'م2', 1600],
        [25, 'توريد وتركيب جرانيت للكاونترات', 'م2', 120],
    ];

    const wsData = [['#', 'وصف البند', 'الوحدة', 'الكمية']];
    testItems.forEach(item => wsData.push(item));

    const wb = xlsx.utils.book_new();
    const ws = xlsx.utils.aoa_to_sheet(wsData);
    xlsx.utils.book_append_sheet(wb, ws, 'BOQ');

    const testFile = path.join(__dirname, 'test_boq_v81.xlsx');
    xlsx.writeFile(wb, testFile);
    console.log(`📝 ملف الاختبار: ${testFile}`);
    return testFile;
}

// Run
(async () => {
    const testFile = createTestBOQ();
    const orchestrator = new ArbaOrchestrator('northern_borders');
    await orchestrator.run(testFile);
})();
