# خطة تكامل مكتب المحاسب — ARBA (إكمال → ربط → تشخيص → إتاحة ZATCA)

مبنية على تشخيصاتنا (خريطة الوحدة + اكتمال البيانات + جاهزية البيانات). كل مرحلة على فرع معزول ببوابة اختبار. لا يُنفَّذ شيء إلا بإذن.

## مبادئ
- **مصدر واحد للحقيقة:** كل الأرصدة تُحسب من القيود المرحّلة، لا من حقول مخزّنة.
- **ذرّية:** كل كتابة متعددة عبر `writeBatch`/`runTransaction`؛ لا fire-and-forget.
- **الترتيب:** M1 الإكمال → M2 الربط → M3 التشخيص → M4 إتاحة ZATCA. (الترقيم الذرّي وحقول Z2 في M1 شرط لـ M4).
- قواعد توكن: diff واحد لكل ملف، نتائج أرقام فقط، اختبار قبل كل دمج.

---

## M1 — الإكمال (البيانات + الجاهزية + وظائف الواجهة)
**فرع** `feat/acc-m1-completion`.

**M1.1 حقول البيانات (من الـspec):** أضِف حقول **[الآن]** و**[Z2 كـplaceholder]** لكل كيان (invoices, InvoiceItem, accounting_clients, ledger, journal+line, purchase_invoices, supplier_payments) — حسب `spec_بيانات_المحاسبة_المستهدفة.md`. + `Address` موحّد، + audit `updatedAt/updatedBy`.

**M1.2 البائع والضريبة كبيانات:** اربط `taxInvoiceService` بـ `company_settings` (احذف ثابت `COMPANY_INFO`)، هيكِل العنوان، أنشئ `tax_settings` (vatRate=15 + فئات S/Z/E/O).

**M1.3 إصلاحات الجاهزية (حرجة):**
- **ترقيم ذرّي عبر Cloud Function** (counter) للفواتير/المشتريات/القيود — **gapless** (يخدم ICV لاحقاً). بدل `length+1`.
- **أرصدة من المصدر:** أعِد حساب `account.balance`، ميزان المراجعة، رصيد العميل، رصيد السجل، و**قائمة الدخل مع فلترة فترة فعلية** (الآن تتجاهلها) — من القيود المرحّلة.
- **تحقق الروابط على الكتابة:** customerId/accountCode/entityId/supplier_payment.invoiceId موجودة فعلاً.
- **استبدال fire-and-forget** بـ batch/transaction + طابور `offlineBufferService`.
- **تحقق rules:** أنواع/enums/مبالغ/حقول إلزامية.

**M1.4 وظائف الواجهة الناقصة:** إضافة/تعديل/تعطيل حساب، إنشاء/ترحيل قيد يدوي من الواجهة، إصلاح زر "PDF" لميزان المراجعة (يصدّر txt)، تصدير قائمة الدخل.

**القبول:** ميزان المراجعة=صفر، أرصدة محسوبة من المصدر تطابق، ترقيم بلا تكرار تحت التزامن، اختبارات خضراء.

---

## M2 — الربط (Integration)
**فرع** `feat/acc-m2-linking`.
- **قبول عرض/مشروع → فاتورة مبيعات + قيد مزدوج** (مدين: ذمم عملاء 110202 / دائن: إيراد مبيعات 4201 + ضريبة مخرجات 2102). ويُملأ `invoice.journalEntryId` (حالياً يبقى فارغاً).
- **استلام دفعة (Tap / تسجيل) → قيد** (مدين: بنك 1101 / دائن: ذمم عملاء).
- **ترقية القيود المفردة الحالية** (اشتراك/RFQ/تخزين) إلى **قيد مزدوج كامل**.
- اربط كل قيد بالفاتورة + العميل (`entityId`) + المشروع (`relatedProjectId`).
**القبول:** دورة عرض→فاتورة→دفعة تنعكس متوازنة في الدفاتر والتقارير.

---

## M3 — التشخيص (Verification)
**فرع** `test/acc-m3-verify`.
- اختبارات end-to-end لدورة المحاسبة الكاملة؛ توازن الدفاتر؛ تطابق الأرصدة من المصدر.
- حالات حافة: حذف عميل/فاتورة، فشل شبكة منتصف العملية، كتابة متزامنة (ترقيم).
- مراجعة بشرية لعيّنة قيود ناتجة عن M2.
**القبول:** كل الاختبارات خضراء؛ لا روابط يتيمة؛ لا انحراف أرصدة.

---

## M4 — إتاحة الربط مع ZATCA (المرحلة 2)
**فرع** `feat/acc-m4-zatca`. (البنية جاهزة: حقول Z2 + الترقيم الذرّي من M1).
- توليد **UBL 2.1 XML** من الفاتورة.
- **التوقيع المشفّر** (ECDSA) + onboarding **CSID** (compliance ثم production).
- **سلسلة PIH** + عداد **ICV** (يعتمد ترقيم M1 الذرّي).
- **API الفسح (B2B)** و**الإبلاغ (B2C)** — على بيئة sandbox أولاً.
- **QR وسوم 6–8** (هاش XML / الشهادة / التوقيع) فوق وسوم 1–5 الموجودة.
**القبول:** اجتياز ZATCA **sandbox validator**؛ فاتورة موقّعة مقبولة.

---

## التبعيات
M1.3 (ترقيم ذرّي) + M1.1 (حقول Z2) ⟶ شرط M4. · M2 يعتمد M1. · M4 آخراً.

## التنفيذ
نبدأ بـ M1 فقط على نسخة معزولة، اختبار، مراجعة معي، ثم دمج — قبل M2. لا شيء يُنفّذ حتى تقول "go".
