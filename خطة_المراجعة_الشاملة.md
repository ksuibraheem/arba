# خطة المراجعة الشاملة — تحقّق من التدقيق ثم خطة تطوير وتنظيم

**المبدأ:** لا نعتمد ادعاءات تقرير التدقيق كما هي (ثبت سابقاً أنه واثق أكثر من اللازم — مثال: وصف `usageTracking` بأنها "ميتة" وهي مستخدمة في 12 ملف). كل ادعاء يُتحقَّق منه آلياً قبل البناء عليه.

---

## القسم أ — فحوصات تحقّق من ادعاءات التقرير (آلية، عبر Antigravity + الوكلاء)

| # | الادعاء في التقرير | الفحص المطلوب (أداة/أمر) | الناتج |
|---|---|---|---|
| A1 | "11 ملف ميت / 220 حي" | لا تعتمد grep يدوي. شغّل **knip** أو **ts-prune** + **depcheck**. راعِ الاستيراد الديناميكي و`lazy()` والمراجع النصّية ونقاط الدخول. | قائمة ميتة موثوقة |
| A2 | "كل المجموعات حية عدا usageTracking" | لكل مجموعة: ابحث عن مسار **قراءة وكتابة فعلي** (`collection(`/`doc(`/`setDoc`/`onSnapshot`) لا مجرد ورود الاسم. | جدول قراءة/كتابة لكل مجموعة |
| A3 🔴 | "العدادات في users، وusageTracking ميتة" | حدّد **أين تُكتب عدادات الحصة فعلاً** (افحص `usageTrackingService.ts`). لو في `users/{userId}` → الحصة قابلة للتلاعب لأن القاعدة تسمح بالتحديث الذاتي. | قرار + إصلاح |
| A4 | "certifyProjectPrice منفصلة" | `grep "certifyProjectPrice"` في كل العميل (httpsCallable). | تأكيد + قرار ربط/حذف |
| A5 | "كل Cloud Functions متصلة" | لكل `onCall` تأكّد من وجود مستدعٍ في العميل؛ لكل `onSchedule` تأكّد أنها منشورة فعلاً في Firebase. | جدول اتصال مؤكد |

## القسم ب — فحوصات أعمق لم يغطّها التقرير

- **B1 أمن القواعد (الأهم):** التقرير لم يدقّق منطق القواعد. أعِد فحص **كل الـ 50 مجموعة** بحثاً عن نفس النمط الخطير `allow create/update: if isAuth()` بلا فحص ملكية. مرشّحون مبدئيون: `supplier_payments` (create: if isAuth())، `internal_metadata` (create,update: if isAuth())، `price_time_series`/`usage_metering`/`audit_trail` (create: if isAuth())، `registration_requests` (create: if true)، و**`users` (update ذاتي كامل)**. الناتج: مصفوفة مخاطر للقواعد + اختبارات سلوكية لكل بند.
- **B2 وثيقة users مفرطة الصلاحية:** أي حقول يجب أن تكون server-only (الدور، الباقة، العدادات، الحالة) لا يجوز للمستخدم تعديلها. هذا جذر مشكلة A3.
- **B3 تغطية الاختبارات:** يوجد اختباران فقط. حدّد المسارات الحرجة غير المختبَرة: الفوترة، المدفوعات، عمولة RFQ، المستورد، التسعير.
- **B4 سلامة البيانات:** اكتشف الكتابات غير الذرية (مثال: `linkQuoteToProject` يكتب مرتين بلا transaction)، والعمليات بلا إعادة محاولة.
- **B5 الأداء/التكلفة:** أنماط قراءة Firestore المكلفة (مثال: `getDashboardStats` يجلب كل المشاريع)، حجم الحزمة، الاعتماديات غير المستخدمة (`depcheck`).
- **B6 صحة الأنواع/البناء:** `tsc --noEmit` على المشروع كامل — هل توجد أخطاء أنواع كامنة لا يكشفها `vite build`؟

## القسم ج — أسئلة قرار لك (تُحدِّد مسار الخطة)

1. `certifyProjectPrice` (شهادة سعر موثّقة): ميزة مطلوبة فنُفعّلها ونربطها، أم تُحذف؟
2. `usageTracking`: ننقل العدادات إليها مع فرضها من Cloud Function (آمن)، أم نحذف المجموعة ونحصّن حقول `users`؟
3. الـ 11 ملف "الميت": هل أيٌّ منها مقصود إبقاؤه (stub/مستقبلي) قبل الحذف؟
4. `arba_system/_archive`: حذف نهائي أم نقل أرشيفي؟
5. الأولوية: جولة تحصين أمني شاملة أولاً، أم تطوير ميزات، أم تنظيف — ما الترتيب؟
6. البيئة: هل الإنتاج حيّ الآن؟ هل يوجد staging للاختبار قبل النشر؟

## القسم د — كيف تتغذّى النتائج في الخطة الكبرى (مراحل مقترحة)

- **مرحلة 1 — تحصين أمني:** إغلاق كل ثغرات القواعد من B1/B2 + إصلاح A3 (الحصة) + Custom Claims للأدوار. بوابة: اختبارات إيموليتر سلوكية خضراء.
- **مرحلة 2 — تطهير الكود:** حذف الميت **بقائمة موثوقة من أداة** (A1) لا من التقرير، + معالجة `certifyProjectPrice` و`usageTracking` حسب قرارك.
- **مرحلة 3 — التغطية الاختبارية:** اختبارات للمسارات الحرجة (B3) + ربطها بـ CI.
- **مرحلة 4 — الأداء والميزات:** معالجة B4/B5 + أي ميزات مقرّرة.
- كل مرحلة على فروع منفصلة، ببوابة "اختبار أخضر + مراجعة" قبل الدمج، ونشر بعد كل مرحلة.

---

## برومبت لـ Antigravity (نفّذ القسمين أ و ب فقط — تحقّق، لا تعدّل)

```
AUDIT VALIDATION RUN — verify, do NOT modify code yet. Report findings only.
Rules: no preamble, no praise, tables/numbers only, ask before any change.

A) Validate the audit's claims:
   - A1: run a real dead-code tool (knip OR ts-prune) + depcheck. Don't trust
     manual grep. Account for dynamic/lazy imports and string refs. Output the
     authoritative dead-file list and how it differs from the report's 11.
   - A2: for each Firestore collection, show actual read AND write call sites.
   - A3 (priority): find where usage/quota counters are actually written. If
     it's users/{userId} fields, flag that the P0 usageTracking hardening
     protected the wrong target and the quota is still client-mutable.
   - A4: grep certifyProjectPrice across client — confirm no caller.
   - A5: map each Cloud Function to its client caller / schedule deployment.

B) Deeper audit the report missed:
   - B1: scan ALL firestore.rules collections for "allow create/update: if
     isAuth()" with no ownership check. Output a risk matrix.
   - B2: list fields in users/{userId} that should be server-only.
   - B6: run tsc --noEmit on the whole project; report error count.

Deliver one findings report (tables). No code changes.
```
