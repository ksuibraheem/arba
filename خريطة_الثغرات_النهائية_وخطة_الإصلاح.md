# الخريطة النهائية للثغرات + خطة الإصلاح المرتّبة — ARBA V10.4.1

مصدرها: تحققاتي المباشرة (V1–V5) + تقارير Antigravity الستة + 3 تحققات إضافية مني. الحالة: **مؤكد** = تحققت منه بنفسي، **مُبلّغ** = من تقرير Antigravity يحتاج تأكيد سريع.

---

## 🔴 حرجة

| ID | الثغرة | الدليل | الحالة |
|---|---|---|---|
| **C1** | **أسرار مكشوفة في حزمة العميل:** `VITE_TAP_SECRET_KEY` (مفتاح دفع سرّي) و`VITE_ADMIN_SECRET_KEY` (مفتاح تخطّي أدمن) ببادئة `VITE_` → تُحقن في JS العميل. | `.env:17,27` · `.env.production:11,16` | ✅ مؤكد |
| **C2** | **تسرّب بيانات مالية للعامة (مؤكد):** `arba_config` قراءة عامة بلا `isAuth`، و`firestoreDataService` يوجّه `chart_of_accounts` و`journal_entries` و`invoice_versions` داخله بمفاتيح مركّبة → **القيود المحاسبية والفواتير مقروءة لأي شخص على الإنترنت بلا تسجيل دخول.** (بخطورة C1). | `firestore.rules:260` · `firestoreDataService.ts:157` | ✅ مؤكد 100% |
| **C3** | **تخطّي الحصة:** العدّادات (`usedProjects`, `usedAIItems`...) في `users/{uid}` والمستخدم يحدّثها ذاتياً. **إصلاح P0 حمى مجموعة `usageTracking` غير المستخدمة.** | `usageTrackingService.ts:326` · `firestore.rules:155` | ✅ مؤكد |
| **C4** | **ميزانية توكنز الذكاء في localStorage** → `localStorage.setItem('_arba_daily_ai_usage','{}')` تصفّرها، ولا فرض من الخادم. **+ مفتاح Gemini مكشوف على العميل** (`process.env.API_KEY`) = سر إضافي محروق. | `budgetGuardian.ts:44` · `App.tsx:1327,1340` | ✅ مؤكد |
| **C5** | **تزوير سجلات منهجي:** 25 مجموعة `create: if isAuth()` بلا فحص ملكية — فواتير، قيود، supplierRFQs، رسائل (انتحال هوية)، حضور، price_time_series (تسميم الذكاء)... | `firestore.rules` (مصفوفة Part 1) | ✅ مؤكد |
| **C6** | **"Zero-Knowledge" وهمي:** `calculations` (المحجوبة عن SuperAdmin) **غير مستخدمة** — حالة التسعير الكاملة في حقل `stateSnapshot` على وثيقة المشروع، **والـ SuperAdmin يقرأها**. | `firestore.rules:82` · Part 5 (calculations=unused) | ✅ مؤكد |
| **C7** | **تصعيد دور جزئي:** المستخدم يعيّن لنفسه `qs_engineer`/`accountant`/`hr` عند الإنشاء (المسكّن يمنع admin/superadmin فقط). | `firestore.rules:66-68` · `rbacService.ts` | ✅ مؤكد |

## 🟠 عالية / متوسطة

| ID | الثغرة | الدليل | الحالة |
|---|---|---|---|
| **H1** | **8** من 11 دالة Callable بلا فحص دور — أي مستخدم (viewer/client) يستدعي المستورد/الأسعار/`createTapCharge`. (تفحص الدور: RFQ×2 + verifyEmployeeCredentials). | Part 3 · `functions/src/index.ts` | ✅ مؤكد (8/11) |
| **H2** | **مسار `quotes` معطّل:** الكود يكتب `/quotes` العلوي، والقاعدة subcollection فقط → **الكتابة مرفوضة بقاعدة الرفض الافتراضية = حفظ العروض لا يعمل في الإنتاج.** (يسبق تعديلاتنا). | `projectService.ts:19,180` · `firestore.rules:126` (داخل projects) | ✅ مؤكد |
| **H3** | كتابات متعددة غير ذرية: ترقية الاشتراك، ربط العرض، شراء التخزين → فساد جزئي عند الفشل. | `billingService.ts:345-393`, `projectService.ts:180-185`, `supplierStorageService.ts:123-134` | ✅ مؤكد |
| **H4** | مزامنة fire-and-forget بـ `.catch(()=>{})` صامت (تسجيل المستخدم، القيود) → فقد بيانات صامت + "نجاح" كاذب في الواجهة. | `authService.ts:146`, `accountingService.ts:415` | ✅ مؤكد |
| **H5** | لا تثبيت `ownerId==auth.uid` عند إنشاء projects/clients/suppliers/external_prices → انتحال ملكية. | `firestore.rules:84,143,...` | ✅ مؤكد |
| **H6** | `trackAgentQuery` webhook عام، تحديد المعدل في الذاكرة → DDoS/إغراق. | Part 3 | 🟡 مُبلّغ |
| **H7** | `registration_requests` create: `if true` → إغراق غير مُصادَق. | `firestore.rules:282` | ✅ مؤكد |

## 🟡 منخفضة / تنظيمية

| ID | البند | الدليل |
|---|---|---|
| L1 | كود ميت: **15 ملف** (لا 11) + نسخ مكررة في `src/` (PrivatePortal, engineeringConstants). | Part 4 |
| L2 | اعتماديات غير مستخدمة في الواجهة: `exceljs`, `tesseract.js`, `firebase-admin`, `pdf-parse/2json`. | Part 4 |
| L3 | 5 أخطاء أنواع تحت `--strict` (أهمها `blueprintIntelligence.ts:512` يتخطّى فحص الأبعاد الإنشائية صامتاً) + `@types/react` ناقص. | Part 6 |
| L4 | تغطية اختبار ~0% للفوترة/المدفوعات/RFQ/المستورد/التسعير؛ `subscription.test.ts` يختبر دوال وهمية مكررة لا الكود الفعلي. | Part 6 |
| L5 | مجموعات غير مستخدمة: `calculations`, `testSessions`, `internal_metadata`, `sweep_history`, `supplier_storage`, `usageTracking` + قاعدة قراءة مكررة في `userRoles`. | Part 1/5 |

---

## خطة الإصلاح المرتّبة (للتنفيذ لاحقاً — لا شيء يُنفَّذ الآن)

### 🚨 المرحلة 0 — طوارئ (ساعات، قبل أي شيء)
- **C1:** انقل `VITE_TAP_SECRET_KEY` و`VITE_ADMIN_SECRET_KEY` **ومفتاح Gemini** خارج العميل كلياً (الأسرار → Cloud Function فقط؛ تحقّق الأدمن → server). **دوّر كل المفاتيح فوراً** لأنها محروقة (في الحزمة وفي تاريخ git).
- **C2 (عاجل):** بيانات مالية مكشوفة للعامة الآن. أضِف `isAuth()` + فحص ملكية لقراءة `arba_config`، **وانقل** `chart_of_accounts`/`journal_entries`/`invoice_versions` لمجموعات محمية خاصة بها (لا تُوجَّه إلى `arba_config` العام).

### المرحلة 1 — تحصين أمني (مُختبَر سلوكياً بالإيموليتر)

#### P1.1 — ملكية + مكافحة تزوير (✅ مدمج — `fix/p1.1-rules-scoped-v2`)

تمّ تثبيت ملكية `ownerId/createdBy == auth.uid` على **6 مجموعات فقط** (لا ~25 كما كان مخطط):

| المجموعة | الحقل المثبّت | الحالة |
|---|---|---|
| `projects` | `ownerId == auth.uid` | ✅ مثبّت + مختبر |
| `clients` | `ownerId == auth.uid` | ✅ مثبّت + مختبر |
| `suppliers` | `ownerId == auth.uid` | ✅ مثبّت + مختبر |
| `supplierRFQs` | `clientId == auth.uid` | ✅ مثبّت + مختبر |
| `supplierStoragePurchases` | `supplierId == auth.uid` | ✅ مثبّت + مختبر |
| `security_alerts` | `reporterId == auth.uid` | ✅ مثبّت + مختبر |

+ `registration_requests` مقيّد بـ `hasAll(['email','name'])` (بدون تسجيل دخول).
+ المجموعات المالية (`invoices`, `ledger_entries`, `chart_of_accounts`, `journal_entries`, `invoice_versions`, `accounting_clients`) مقيّدة بـ `isAdmin()`.

**السبب**: بقية المجموعات تمرّ عبر طبقة المزامنة (`firestoreInitializer` / `firebaseService` / `firestoreDataService`) التي تحمّل سجلات **كل المستخدمين** إلى `localStorage` ثم تعيد تشغيلها — فتُرفض لأن `ownerId`/`createdBy`/`senderId` ≠ `auth.uid`.

الاختبارات: **69 نجح، 13 مؤجّل، 0 فشل** (بما فيها 15 fixture test).

#### P1.2 — إصلاح نموذج الهوية (⏳ قادم)

مجموعتان معلّقتان بسبب عدم تطابق `employee.id` مع `auth.uid`:

| المجموعة | الحقل | المشكلة |
|---|---|---|
| `attendance` | `employeeId` | `employee.id` داخلي ≠ `auth.uid` |
| `discount_requests` | `requestedBy` | نفس المشكلة |

**الحل**: إصلاح نموذج الموظف ليربط `employee.id` بـ `auth.uid`، ثم تفعيل التثبيت.

#### P1.6 — مجموعات الكتابة النظامية → Admin SDK (⏳ قادم)

المجموعات التالية مؤجّلة لأنها تُكتب بواسطة النظام (لا المستخدم) أو بها عدم تطابق في الحقول:

| المجموعة | سبب التأجيل |
|---|---|
| `external_suppliers` | `createdBy: 'system'` في بيانات العيّنة |
| `external_prices` | نفس السبب |
| `support_tickets` | `userId` يُضبط على `'guest'` افتراضياً |
| `notifications` | لا حقل `userId` في الحمولة |
| `connect_forms` / `connect_notes` / `connect_gallery` | الحقل `createdBy.id` وليس `userId` |
| `supplier_products` | البيانات التجريبية تستخدم معرّفات داخلية |
| `supplier_reviews` | نفس المشكلة |
| `data_correction_requests` / `invoice_edit_requests` | لا حقل `userId` |
| `action_logs` / `price_time_series` / `usage_metering` | كتابة نظامية |
| `audit_trail` / `internal_metadata` / `sweep_history` / `upgradeHistory` | كتابة نظامية |
| `payments` | الحقل `createdBy` وليس `userId`؛ المزامنة تعيد تشغيل سجلات الآخرين |
| `purchase_invoices` / `supplier_payments` | المزامنة تحمّل كل السجلات |
| `connect_messages` / `connect_mail` | المزامنة تحمّل الرسائل المستقبَلة أيضاً |
| `auth_users` | معرّف الوثيقة = `user_timestamp_xxx` ≠ `auth.uid` |

**الحل**: نقل كتابة هذه المجموعات إلى Admin SDK (Cloud Functions) ثم تفعيل `allow create: if false` على العميل.

---

- **C3:** نقل العدّادات وفرض الحصة إلى Cloud Function؛ منع المستخدم من تعديل حقول الحصة في `users`.
- **C4:** نقل ميزانية الذكاء إلى الخادم.
- **C6:** تشفير `stateSnapshot` من جهة العميل أو نقله لمجموعة محجوبة عن SuperAdmin.
- **C7:** Custom Claims للأدوار (إزالة كتابة العميل لـ `userRoles`).
- **H1:** إضافة فحص دور داخل الدوال الـ Callable.

### المرحلة 2 — سلامة البيانات
- **H3:** تحويل الكتابات المتعددة إلى `writeBatch`/`runTransaction`.
- **H4:** استبدال fire-and-forget بطابور الإعادة (offlineBufferService الذي بنيناه).
- **H2:** حسم مسار `quotes` (subcollection موحّد) وإصلاح الكود.

### المرحلة 3 — تطهير
- حذف الـ15 ملفاً ميتاً + الاعتماديات الزائدة (L1/L2)، إصلاح أخطاء الأنواع الـ5 وتفعيل `strict` (L3)، حذف المجموعات غير المستخدمة من القواعد (L5).

### المرحلة 4 — اختبارات
- اختبارات حقيقية للفوترة/المدفوعات/RFQ/المستورد/التسعير + ربطها بـ CI (L4).

---

## ⚠️ مشكلات معروفة (KNOWN ISSUES)

| المشكلة | التأثير | الحالة |
|---|---|---|
| **مزامنة `subscriptions` تفشل صامتة لغير المدير** | `firestoreInitializer.ts:93` يحمّل `subscriptions` → `arba_subscriptions`، لكن قاعدة `create: if isAdmin()` ترفض الكتابة لغير المدير. الخطأ يُلتقط صامتاً (`catch {}` في L126). البيانات تبقى في `localStorage` (لا فقد). | موجودة قبل P1.1 — ليست انحداراً |
| **3 خلل `resource.data` null عند الإنشاء (مُصلَح)** | `supplier_storage` / `company_settings` / `company_employees` كانت تستخدم `allow write` مع `resource.data.xxx` → فشل عند الإنشاء لأن الوثيقة غير موجودة بعد. **تمّ الإصلاح** بفصل `allow write` إلى `create` + `update/delete`. | ✅ مُصلَح في P1.1-v2 |
| **حسابات localStorage القديمة بلا مسار استعادة كلمة مرور** | الحسابات المنشأة عبر `services/authService.ts` (المسار القديم) لا تملك استعادة كلمة مرور بعد حذف الفرع المحلي الذي كان يولّد كلمة مرور مؤقتة. **هذا مقصود:** لا يجوز تغيير كلمة مرور بلا إثبات ملكية. ترحيل هذه الحسابات إلى Firebase Auth = بند **P1.2**. | ⏳ مقصود — ينتظر P1.2 |
| **إصلاح: تسريب وجود البريد عبر استعادة كلمة المرور (مُصلَح)** | `resetPasswordWithFirebase` كان يُميّز بين `auth/user-not-found` وغيره في الاستجابة → كشف ما إذا كان البريد مسجلاً. + فرع محلي كان يولّد كلمة مرور مؤقتة ويعرضها. **تمّ الإصلاح:** الدالة تُرجع `{ success: true }` لكل النتائج. الرسالة الموحّدة: «إن كان البريد مسجلاً لدينا فستصلك رسالة استعادة.» | ✅ مُصلَح — `feat/auth-password-flows` (مدمج `cd30df0`) |

---

## حالة الاعتماد: ✅ الخريطة مؤكدة 100%
كل البنود تحققت من الكود مباشرة. نتائج الإغلاق:
1. **C2 مؤكد:** `arba_config` عام + توجيه `chart_of_accounts`/`journal_entries`/`invoice_versions` إليه → بيانات مالية مكشوفة للعامة.
2. **H2 مؤكد:** `quotes` يُكتب في الجذر والقاعدة subcollection فقط → الكتابة مرفوضة (الميزة معطّلة).
3. **C4/H1/H3/H4 مؤكدة** (+ مفتاح Gemini مكشوف على العميل؛ H1 = 8/11 لا 9/11).
4. **P1.1 مدمج:** 6 مجموعات مثبّتة + 13 مؤجّلة (P1.2 + P1.6). 69 نجح، 13 مؤجّل، 0 فشل.

> **تصعيد:** C2 (تسرّب مالي عام) يرتقي ليصبح مع C1 على رأس مرحلة الطوارئ.
