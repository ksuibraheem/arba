# Firebase App Hosting Configuration
# تكوين Firebase App Hosting

runConfig:
  # Runtime environment
  runtime: nodejs20

  # Build command — runs before start
  buildCommand: npm run build
  
  # Start command
  commandLine: npm run start
  
  # Environment variables
  environmentVariables:
    - variable: NODE_ENV
      value: production
