# ملخص شامل — حالة مشروع ARBA ونقطة الاستئناف

**آخر تحديث:** 2 أغسطس 2026 · **الغرض:** فتح صفحة/جلسة جديدة والاستئناف بثقة.
**المنصة:** ARBA Pricing Platform (React/TypeScript/Vite/Firebase + Cloud Functions). العمل يتم على نسخ معزولة/فروع، ببوابة اختبار قبل الدمج، ومراجعة الكود قبل الموافقة.

---

## 1) ما تم إنجازه

### أ. المزامنة وحفظ البيانات — ✅ مكتمل ومدموج ومنشور
- `offlineBufferService` (معالجة serverTimestamp + إصلاح بق الطابور اللانهائي)، `projectService` (offline + كشف تعارض `updateProjectChecked`)، `draftAutosaveService` (حفظ تلقائي محلي كل 10ث).
- Version checker + Crash snapshot (ErrorBoundary).
- اختبارات: **sync 23/23**.

### ب. الأمن P0 (طوارئ) — ✅ مكتمل ومدموج ومنشور
- إزالة الأسرار من العميل (Tap secret + admin key + Gemini) + **تدوير المفاتيح مطلوب/تم من طرفك**.
- `arba_config` صار يشترط `isAuth()`؛ المجموعات المالية (chart_of_accounts/journal_entries/invoice_versions) نُقلت لـ top-level بقاعدة `isAdmin()`.
- إغلاق: self-assign admin في `userRoles`، calculations، priceHistory، payments، usageTracking (جزئياً).
- اختبارات سلوكية بالإيموليتر: **15/15**.

### ج. الأمن P1.1 (منع التزوير) — ⚠️ جاهز وصحيح لكن **غير مُلتزم/غير منشور**
- قواعد ملكية على كل `create/update` في ~25 مجموعة، بالحقل الصحيح (`createdBy`/`senderId`/`employeeId`... بعد تصحيح خطأ `userId`)، تثبيت `ownerId`، حصر `quotes`، إصلاح `registration_requests`.
- اختبارات: **80/80** (سابقاً). **لكن التعديلات لا تزال dirty في working tree** — لم تُلتزم ولم تُنشر.

### د. المحاسبة M1 (الإكمال) — ✅ مكتمل ومختبَر لكن **غير مدموج**
- 68 حقلاً جديداً عبر 12 كياناً ([الآن] + [Z2] لـ ZATCA)، نوع `Address` موحّد، `tax_settings`، ربط البائع بـ `company_settings` (إزالة الثابت).
- ترقيم ذرّي عبر Cloud Function `getNextNumber` (**غير منشورة** → يسقط لـ`PROV-`).
- إعادة حساب الأرصدة من القيود المرحّلة (ميزان/دخل بفلترة فترة/رصيد عميل)، تحقق مرجعي، استبدال fire-and-forget بالطابور.
- واجهة: CRUD حسابات، إنشاء/ترحيل قيد يدوي، تصدير PDF لميزان المراجعة وقائمة الدخل.
- اختبارات: **accounting 38/38** + **sync 23/23**. الفرع `feat/acc-m1-completion` (5 commits) غير مدموج.

---

## 2) الحالة الحالية (git / النشر / الاختبارات)
| العنصر | الحالة |
|---|---|
| main | P0 + المزامنة (P1/P2/P3/P2a) + P5 cleanup مدموجة |
| P1.1 قواعد التزوير | **dirty غير مُلتزم** (النسخة الصحيحة) — غير منشور |
| M1 محاسبة | فرع `feat/acc-m1-completion` — **غير مدموج** |
| `getNextNumber` Cloud Function | **غير منشورة** |
| tsc / vite | نظيف / أخضر |
| sync / accounting / firestore-rules | 23/23 · 38/38 · (الإيموليتر يحتاج تشغيل يدوي) |
| ملفات .md للخطط/التشخيص | untracked (مرجعية) |

---

## 3) خريطة الثغرات — حالة الإغلاق
- ✅ **مغلقة ومنشورة:** الأسرار، arba_config العام، تسرّب المالية، self-assign admin، تحصين calculations/priceHistory/payments.
- ⚠️ **مُصلَحة لكن غير منشورة (حيّة الآن):** تزوير السجلات في ~25 مجموعة (invoices/ledger/رسائل/حضور...) — **P1.1**. أولوية النشر.
- 🔴 **لا تزال مفتوحة (مؤجّلة P1.2–P1.5):**
  - **C3 الحصة:** العدّادات في `users/{uid}` قابلة لتعديل المستخدم ذاتياً → تخطّي الباقة.
  - **C7 الأدوار:** self-assign لدور `qs_engineer`/غيره ممكن (Custom Claims مطلوبة).
  - **H1 الدوال Callable:** 8/11 بلا فحص دور.
  - **C6 stateSnapshot:** كامل حالة التسعير مكشوف للـ SuperAdmin.

---

## 4) المتبقّي (خريطة الطريق)

**فوراً — مصالحة (Reconciliation):**
1. التزام + اختبار + دمج + نشر **قواعد P1.1** (تغلق ثغرات حيّة).
2. دمج **M1** + نشر `getNextNumber`.

**الأمن المؤجّل:**
- P1.2 Custom Claims (+ استعادة وصول المحاسب غير-الأدمن للمالية).
- P1.3 تأمين الحصة على الخادم + قفل حقول `users`.
- P1.4 فحص الدور داخل الدوال الثماني.
- P1.5 عزل `stateSnapshot`.

**المحاسبة:**
- **M2 الربط:** قبول عرض/مشروع → فاتورة + قيد مزدوج؛ الدفعات (Tap/تسجيل) → قيود؛ ترقية القيود المفردة لمزدوجة؛ ربط `journalEntryId`/المشروع؛ **مصالحة أرقام `PROV-`**.
- **M3 التشخيص:** اختبارات end-to-end + توازن الدفاتر + حالات الحافة.
- **M4 إتاحة ZATCA (المرحلة 2):** XML/UBL 2.1، توقيع مشفّر/CSID، PIH + ICV (يعتمد ترقيم M1)، API الفسح/الإبلاغ (sandbox أولاً)، QR وسوم 6–8.

---

## 5) ثوابت هندسية (تحكم كل عمل قادم)
- **مصدر واحد للحقيقة:** كل الأرصدة تُحسب من القيود المرحّلة.
- **ترقيم ذرّي بلا فجوات** عبر Cloud Function (أساس ICV لـ ZATCA).
- **لا fire-and-forget:** كل كتابة متعددة عبر batch/transaction + طابور offline.
- إعادة استخدام `company_settings` للبائع؛ نسبة VAT كإعداد.
- الأدوار عبر Custom Claims (لا كتابة العميل لـ userRoles).
- كل مرحلة: فرع معزول + **وكيل QA منفصل يكتب ويشغّل الاختبارات** (لا يختبر المنفّذ عمله) + مراجعة الكود قبل الدمج.

---

## 6) ملفات مرجعية في المستودع
- `خريطة_الثغرات_النهائية_وخطة_الإصلاح.md` — كل الثغرات وحالتها.
- `خطة_تكامل_مكتب_المحاسب.md` — خطة M1→M4.
- `spec_بيانات_المحاسبة_المستهدفة.md` — نموذج البيانات المستهدف.
- `خطة_P1_التحصين_لـAntigravity.md` — تفاصيل P1.1–P1.5.
- `status_report_2026-08-02.md` — حالة git التفصيلية.

---

## 7) الخطوة التالية فوراً (أمر جاهز لـ Antigravity)
```
Resume reconciliation. Keep P1.1 (rules) and M1 (accounting) as SEPARATE branches.
Do NOT deploy until I confirm.
1) Commit the uncommitted firestore.rules + firestore-rules.test.mjs (correct
   createdBy/isAdmin anti-forgery rules) onto fix/p1.1-rules-ownership off main. Start
   the firestore emulator, run tests/firestore-rules.test.mjs — report numbers. If
   green, merge into main.
2) Merge feat/acc-m1-completion (M1) into main.
3) Run tsc --noEmit, vite build, sync.test.ts, accounting-qa.test.ts — report numbers.
4) Prepare (don't run) deploy: firebase deploy --only firestore:rules ; and
   firebase deploy --only functions:getNextNumber — then ask me before deploying.
Report numbers + git log of main. No M2 yet.
```
بعد المصالحة والنشر نكون على أرض صلبة، ثم ننطلق **M2 (الربط)**.
