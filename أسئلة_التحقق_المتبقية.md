# أسئلة التحقق المتبقية — لإغلاق الخريطة 100% (تحليل فقط)

6 تحققات مركّزة بأدلة `file:line`. لا تعديل كود. إجابات قصيرة (مؤكد/غير مؤكد + دليل).

## Q1 — C2: محتوى `arba_config` وقراءته العامة
- هل قاعدة قراءة `arba_config` (سطر 259-260) بلا `isAuth()` فعلاً (قراءة لغير المُصادَقين)؟
- ما الـ docIds / المجموعات المنطقية التي يوجّهها `firestoreDataService` للتخزين داخل `arba_config` (المفاتيح المركّبة)؟ وهل أيٌّ منها مالي/حساس (chart_of_accounts, journal_entries, invoice_versions)؟
- **الحسم:** هل توجد بيانات مالية قابلة للقراءة علناً؟ نعم/لا + الأدلة.

## Q2 — H2: تداخل مسار `quotes`
- هل `match /quotes/{quoteId}` (سطر 126) **داخل** بلوك `projects/{projectId}` (subcollection) أم على الجذر؟
- أين يكتب الكود العرض فعلاً: `projectService.ts` (`QUOTES_COL`) — جذر `/quotes` أم `/projects/{id}/quotes`؟
- **الحسم:** هل كتابة العروض تُرفض بقاعدة الرفض الافتراضية، أم تكتب في مسار بلا حماية؟

## Q3 — C4: ميزانية توكنز الذكاء
- في `budgetGuardian.ts`: أين تُخزَّن العدادات اليومية؟ (`localStorage` مفتاح `_arba_daily_ai_usage`؟) وهل يوجد أي فرض من الخادم قبل استدعاء Gemini؟
- **الحسم:** هل `localStorage.setItem('_arba_daily_ai_usage','{}')` يصفّر الميزانية؟ نعم/لا.

## Q4 — H1: غياب فحص الدور في الدوال Callable
- لكل دالة `onCall` في `functions/src/`: هل تتحقق من **الدور** (لا مجرد `request.auth`)؟ أكّد العدد (9/11 بلا فحص دور) واذكر أي الدوال تفحص الدور فعلاً.

## Q5 — H3: الكتابات غير الذرية
- أكّد بالأدلة أن هذه تكتب وثائق متعددة تتابعياً بلا `writeBatch`/`runTransaction`:
  ربط العرض (`projectService.ts:172-188`)، ترقية الاشتراك (`billingService.ts:345-393`)، شراء التخزين (`supplierStorageService.ts:123-135`).

## Q6 — H4: مزامنة fire-and-forget الصامتة
- أكّد أن هذه تطلق كتابة Firestore بلا `await` ومع `.catch(()=>{})` صامت → "نجاح" كاذب في الواجهة:
  `authService.ts:142-147` (تسجيل المستخدم)، `accountingService.ts:410-418` (القيود).

---

## ENGLISH PROMPT (paste to Antigravity)
```
VERIFICATION RUN — analyze only, no code changes. Short answers: confirmed/not +
file:line evidence. Close these 6 open items:

Q1 (arba_config): Is the read rule at firestore.rules:259-260 truly public (no
   isAuth)? Which docIds/logical collections does firestoreDataService route INTO
   arba_config (compound keys)? Are any financial/sensitive (chart_of_accounts,
   journal_entries, invoice_versions)? Verdict: is financial data publicly readable?
Q2 (quotes path): Is "match /quotes/{quoteId}" (line 126) nested inside
   projects/{projectId} (subcollection) or top-level? Where does projectService
   QUOTES_COL actually write — /quotes or /projects/{id}/quotes? Verdict: are quote
   writes denied by default-deny, or written to an unprotected path?
Q3 (AI budget): In budgetGuardian.ts where are daily token counters stored
   (localStorage _arba_daily_ai_usage?) and is there ANY server enforcement before
   Gemini calls? Verdict: does clearing localStorage reset the budget?
Q4 (callable role checks): For each onCall in functions/src/, does it check ROLE
   (not just request.auth)? Confirm the 9/11 figure; name the ones that do check.
Q5 (non-atomic writes): Confirm with evidence these write multiple docs sequentially
   without writeBatch/runTransaction: projectService.ts:172-188 (quote link),
   billingService.ts:345-393 (upgrade), supplierStorageService.ts:123-135 (storage).
Q6 (fire-and-forget): Confirm these fire a Firestore write without await and a silent
   .catch(()=>{}) (false success in UI): authService.ts:142-147 (user register),
   accountingService.ts:410-418 (ledger).

One short findings list. No code changes.
```
